spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/skillforge
    username: skillforge
    password: skillforge
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
